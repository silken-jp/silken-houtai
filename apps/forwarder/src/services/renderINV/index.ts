import renderBL from './renderBL';
import renderINV from './renderINV';
import renderINV2 from './renderINV2';

const render = {
  BL: renderBL,
  INV: renderINV,
  INV2: renderINV2,
};

export { renderBL, renderINV, renderINV2 };
export default render;
